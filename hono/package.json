{
  "name": "hono-worker-template",
  "description": "your next hono-worker package",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir=build",
    "deploy": "wrangler publish",
    "dev": "wrangler dev --local --live-reload ./src/app.ts",
    "dev:node": "DEBUG=* tsx src/node-server",
    "lint": "eslint src",
    "lint:fix": "eslint --fix src",
    "format": "dprint fmt",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "test:watch": "jest --watch",
    "typecheck": "tsc --noEmit",
    "typecheck:watch": "tsc --watch --noEmit"
  },
  "dependencies": {
    "@hono/node-server": "^1.12.2",
    "hono": "^4.5.10"
  },
  "devDependencies": {
    "@eslint-react/eslint-plugin": "*",
    "@eslint/js": "*",
    "@tsconfig/strictest": "*",
    "@types/jest": "*",
    "debug": "^4.3.6",
    "dprint": "*",
    "eslint": "*",
    "eslint-plugin-import-x": "*",
    "jest": "*",
    "ts-jest": "*",
    "ts-node": "*",
    "tslib": "^2",
    "tsx": "*",
    "typescript": "*",
    "typescript-eslint": "*",
    "wrangler": "^3.73.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
